use iot;
create table tf(
id int not null auto_increment,
tds float default null,
tempe float default null,
dist float default null,
boton int default 0,
fotores float default 0,
fecha datetime default CURRENT_TIMESTAMP,
primary key(id)
);


create table t1(
id int not null auto_increment,
boton int default 0,
fecha datetime default CURRENT_TIMESTAMP,
primary key(id)
);

create table t2(
id int not null auto_increment,
dist float default null,
fotores float default 0,
fotoval int default 0,
fecha datetime default CURRENT_TIMESTAMP,
primary key(id)
);

create table t3(
id int not null auto_increment,
tds float default null,
tempe float default null,
fecha datetime default CURRENT_TIMESTAMP,
primary key(id)
);

ALTER table tf
Add column fotoval int default null


INSERT into tf (tds,tempe, dist, boton, fotores, fotoval) values (7, 10, 200, 1, 1, 250);

